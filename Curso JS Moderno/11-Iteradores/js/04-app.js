let i = 0; // Inicializar el while

while (i < 10) { // Condicion
    if (i % 2 === 0) {
        console.log(`Número ${i} PAR`);
    } else {
        console.log(`Número ${i} IMPAR`);
    }
    i++; // Incremento
}